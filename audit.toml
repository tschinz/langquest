# cargo-audit configuration.
[advisories]
ignore = [
  # bincode 1.x is unmaintained. It is pulled in transitively by `syntect`
  # (syntax highlighting), not used directly, and this is an "unmaintained"
  # notice rather than a vulnerability. Remove once syntect drops bincode.
  "RUSTSEC-2025-0141",
]
